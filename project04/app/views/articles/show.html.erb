<p id="notice"><%= notice %></p>

<h1>Articl.es</h1>
<h3>... what's "Articl" in Spanish anyway?</h3>
		
<div class = "view_buttons">
		<td><%= link_to 'list all articles', articles_path %></td>
		<td><%= link_to 'edit', edit_article_path(@article) %></td>
		<td><%= link_to 'destroy', @article, confirm: 'Are you sure?', method: :delete %></td>
		<td><%= mail_to "guy@gmail.com", 'email', :subject => @article.title, :body => article_url(@article)  %></td>
</div>

<h4>
  <%= @article.title %>
</h4>

<p>
  by
  <%= @article.author %>. <%= pluralize(@article.edit_count, "edit") %> since <%= @article.creation_date %>
</p>

<p>
  <%= @article.body %>
</p>

